name: siniestros_sismo
description: App móvil para registro offline de siniestros por sismo

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  uuid: ^4.1.0
  http: ^1.1.0
  geolocator: ^10.1.0
  intl: ^0.18.1
  connectivity_plus: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
